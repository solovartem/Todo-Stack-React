const server=require("./config/server");require("./config/database");const dbConfig=require("../src/config/database"),Sequelize=require("sequelize"),connection=new Sequelize(dbConfig),Todo=require("./api/todo/Todo");Todo.init(connection),connection.authenticate().then(()=>console.log("Connection has been established successfully.")).catch(err=>console.log("Unable to connect to the database:",err)),module.exports=Sequelize;