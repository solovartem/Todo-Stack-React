const port=3003,express=require("express"),routes=require("./routes"),server=express(),cors=require("cors");server.use(cors()),server.use(express.json()),server.use(routes),server.listen(3003,()=>console.log("BACKEND is running on port 3003."));